# --- Files intentionally kept OUT of the public repo ---
# Medium draft (publish on Medium instead, with canonical pointing to the page)
Medium_Dollar_Quote_Bypass.md
# Offensive companion write-up (anonymized, but not for public hosting)
Dollar_Quote_SQL_Injection_Attack.md
Dollar_Quote_Bypass_Research_Paper.md
# --- Local LaTeX toolchain (tectonic binary + cache), never push ---
.tools/

# --- LaTeX build artifacts (keep the .tex and the compiled .pdf) ---
*.aux
*.log
*.out
*.toc
*.xdv
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg
*.run.xml

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/
